// Firebase project configuration for cloud sync.
// Create a Firebase project and paste your config values here.
// These keys are safe to commit — security is enforced by Firestore rules.
export const firebaseConfig = {
    apiKey: "REPLACE_ME",
    authDomain: "REPLACE_ME.firebaseapp.com",
    projectId: "REPLACE_ME",
    storageBucket: "REPLACE_ME.appspot.com",
    messagingSenderId: "REPLACE_ME",
    appId: "REPLACE_ME"
};
